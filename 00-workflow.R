# run "source" function on a R file basically re-runs the r-source code
#when sending to someone else can send code with comments explaining each source code

#ex: first we read and wrangle the data to get just 1980s
source("R/01-wrangle.R")

#ex: Then we 